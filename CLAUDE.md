# CLAUDE.md

This file is a pointer for agents that read `CLAUDE.md`.

All actionable repository guidance lives in `AGENTS.md`.

- Read `AGENTS.md` immediately, before planning, before execution, and before any other action.
- Follow `AGENTS.md` as the canonical instruction source.
- If instruction updates are needed, update `AGENTS.md` and keep this file as a pointer only.
